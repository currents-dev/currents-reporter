describe('onboarding', () => {
  it('shows the welcome carousel', async () => {
    await expect(true).toBe(true);
  });

  it('creates a wallet @smoke', async () => {
    await expect(true).toBe(true);
  });
});
