describe('login', () => {
  it('opens the wallet with a PIN @smoke', async () => {
    await expect(true).toBe(true);
  });

  it('rejects a wrong PIN', async () => {
    await expect(true).toBe(true);
  });
});
